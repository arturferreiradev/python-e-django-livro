# Operador ternario é uma instrução if condensada em uma linha, porém deve ser evitado pois é contrário a uma das "regras" do Zen do Python: explícito é melhor que implícito

idade = 20
print('menor de idade' if idade < 18 else 'maior de idade')